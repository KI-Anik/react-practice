

const SecondCard = ({name, age}) => {

    return (
        <div className="card">
            <p>Name : {name}</p>
            <p>Age : {age}</p>
        </div>
    );
};

export default SecondCard;